[package]
name = "aigame"
version = "0.1.0"
description = "AIGAME desktop shell"
authors = ["AIGAME"]
edition = "2021"

[lib]
name = "aigame_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
